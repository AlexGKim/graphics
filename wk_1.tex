\documentclass[tikz, border=10pt]{standalone}
\usepackage{amsmath}
\usepackage{tikz}
\usepackage{graphicx}
\usetikzlibrary{decorations.pathmorphing, arrows.meta, calc, backgrounds}

\definecolor{navybg}{RGB}{13,27,42}
\definecolor{labelcol}{RGB}{220,220,220}

\begin{document}
\begin{tikzpicture}[
    background rectangle/.style={fill=navybg},
    show background rectangle,
]

\path[use as bounding box] (0,0) rectangle (16,9);

% ════════════════════════════════════════════════════════
% 0. TITLE
% ════════════════════════════════════════════════════════
\node[text=labelcol, font=\large\bfseries] at (8.0, 8.3)
    {Source First-Order Coherence \textemdash\ Wiener-Khinchin Theorem \textemdash\
     $g^{(1)}(\tau) \Leftrightarrow S(\nu)$};

% ════════════════════════════════════════════════════════
% 1. LEFT SIDE: DERIVATION EQUATIONS
% ════════════════════════════════════════════════════════
\node[text=labelcol, font=\small, align=left, anchor=north west, text width=8.0cm] at (0.5, 7.5) {
    $g^{(1)}(\tau) = \frac{\Gamma(\tau)}{\Gamma(0)} = \frac{\langle E^{*}(t)E(t+\tau)\rangle}{\langle E^{*}(t)E(t)\rangle}$ \\[4pt]
    $\Gamma(\tau) = \langle E^{*}(t)E(t+\tau)\rangle$ \\[4pt]
    $S(\nu) = \lim\limits_{T\to\infty}\frac{1}{2T}\left\langle\left|\int_{-T}^{T}E(t)e^{-i2\pi\nu t}dt\right|^{2}\right\rangle$ \\[4pt]
    $S(\nu) = \lim\limits_{T\to\infty}\frac{1}{2T}\int_{-T}^{T}\int_{-T}^{T}\Gamma(t_{1}-t_{2})e^{-i2\pi\nu(t_{1}-t_{2})}dt_{1}dt_{2}$ \\[4pt]
    $S(\nu) = \int_{-\infty}^{\infty}\Gamma(\tau)e^{-i2\pi\nu\tau}d\tau$
};

% ════════════════════════════════════════════════════════
% 2. RIGHT SIDE: PLOTS
% ════════════════════════════════════════════════════════
\node[anchor=north west, inner sep=0] at (9.0, 7.5) {
    \includegraphics[width=6.5cm, height=7.5cm, keepaspectratio]{figs/wk_plots.pdf}
};

\end{tikzpicture}
\end{document}
